[project]
name = "simctl"
version = "0.1.0"
description = "CLI for eth-ec-broadcast simulations"
requires-python = ">=3.11"
dependencies = [
    "click>=8.0",
    "pyyaml>=6.0",
    "pydantic>=2.0",
]

[project.scripts]
simctl = "simctl.main:cli"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.basedpyright]
typeCheckingMode = "standard"

[tool.hatch.build.targets.wheel]
packages = ["simctl"]
